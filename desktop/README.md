# Kim Desktop

Native Tauri client for the hosted Kim agent. It keeps the agent on `app.vishalojha.me` while giving the desktop a proper native window instead of another browser tab.

```bash
npm install
npm run tauri dev
```

The release workflow builds installers for Linux, Windows, and macOS. Open the `kim-desktop-latest` prerelease on GitHub after the workflow completes.
