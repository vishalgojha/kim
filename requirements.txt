websockets>=13
httpx>=0.27
pyyaml>=6.0
python-dotenv>=1.0
# Optional pure-Python D-Bus client used for Wayland screen capture through
# the XDG desktop portal (only needed on the laptop relay).
dbus-next>=0.2.3
elevenlabs>=2.68
psutil>=5.9
sounddevice>=0.5
google-api-python-client>=2.180
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2
# Windows desktop relay (computer-agent control): screen capture, input, windows.
# Install on the Windows machine running `py -m aurora desktop`.
pillow>=10.0
pyautogui>=0.9.54
pygetwindow>=0.0.9
pytesseract>=0.3.10   # needs tesseract.exe (https://github.com/UB-Mannheim/tesseract/wiki)
